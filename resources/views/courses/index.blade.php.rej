@@ -1013,7 +1013,21 @@
                         </tbody>
                     </table>
                 </div>
+                @if($allCourses->hasPages())
+                    <div class="card-footer bg-white border-top-0">
+                        <div class="d-flex justify-content-between align-items-center">
+                            <div class="text-muted">
+                                Showing <span class="fw-semibold">{{ $allCourses->firstItem() }}</span> to 
+                                <span class="fw-semibold">{{ $allCourses->lastItem() }}</span> of 
+                                <span class="fw-semibold">{{ $allCourses->total() }}</span> courses
+                            </div>
+                            <div>
+                                {{ $allCourses->appends(request()->query())->links() }}
+                            </div>
+                        </div>
+                    </div>
+                @endif
             </div>
         </div>
     </div>
-
+    
